# Jekyll configuration for GitHub Pages
# IMPORTANT: This file disables Jekyll processing when using GitHub Actions deployment
# The site is built by Vite and deployed via GitHub Actions, not Jekyll

include: []
exclude:
  - "*"

